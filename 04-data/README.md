# Data

Folder ini menyimpan data, backup, seed, dan export.

- `backups/` — Backup otomatis/manual
- `seeds/` — Data awal untuk dev/test
- `exports/` — Export hasil running

**Catatan:** File data besar/biner tidak di-commit. Hanya `.gitkeep` dan `README.md`.
